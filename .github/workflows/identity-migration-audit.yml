name: Hack identity migration audit

on:
  pull_request:
    branches: [main]
    paths:
      - "data/evidence-decisions.json"
      - "data/hack-identity-migrations.json"
      - "data/life-os-content/**"
      - "life-os/datasets/hacks.json"
      - "scripts/audit-hack-identity-migrations.mjs"
      - ".github/workflows/identity-migration-audit.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - name: Discover exact-provenance identity mappings
        run: node scripts/audit-hack-identity-migrations.mjs --discover
