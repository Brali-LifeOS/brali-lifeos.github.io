import { mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";

const root = process.cwd();
const base = "https://brali-lifeos.github.io";
const evidence = JSON.parse(await readFile(path.join(root, "life-os/datasets/evidence.json"), "utf8"));
const indexing = JSON.parse(await readFile(path.join(root, "life-os/datasets/indexing.json"), "utf8"));
const normalizations = JSON.parse(await readFile(path.join(root, "life-os/datasets/editorial-normalizations.json"), "utf8"));
const claimDebt = JSON.parse(await readFile(path.join(root, "life-os/datasets/claim-debt.json"), "utf8"));
const knowledgeOntology = JSON.parse(await readFile(path.join(root, "data/knowledge-ontology.json"), "utf8"));
const counts = evidence.counts ?? {};
const claimCounts = claimDebt.counts ?? {};
const canonical = `${base}/life-os/methodology/`;
const topicTitleById = new Map((knowledgeOntology.topics ?? []).map(topic => [topic.id, topic.title]));

const claimCategoryRows = (claimDebt.category_definitions ?? []).map((category) => {
  const count = claimCounts.by_category?.[category.id] ?? 0;
  const mode = category.enforced ? "enforced" : "monitor-only";
  return `<li><strong>${category.id}</strong> · ${mode} · ${count} record${count === 1 ? "" : "s"}. ${category.description}</li>`;
}).join("");
const topicDebtRows = Object.entries(claimCounts.debt_by_topic ?? {})
  .sort((left, right) => (right[1] - left[1]) || left[0].localeCompare(right[0]))
  .slice(0, 12)
  .map(([topicId, count]) => `<li><strong>${topicTitleById.get(topicId) ?? topicId}</strong> · <code>${topicId}</code> · ${count} debt record${count === 1 ? "" : "s"}</li>`)
  .join("");
const topicDebtGroupCount = Object.keys(claimCounts.debt_by_topic ?? {}).length;

const body = `<p class="eyebrow">How Brali thinks</p><h1>Try the move. Understand the mechanism. Know where the evidence stops.</h1><p class="lead">Brali is a library of small protocols, not a shrine to productivity tricks. A useful page should tell you what to try, why the move is plausible, what signal to watch, and what would make you change or stop it.</p><div class="grid two"><article class="card"><span class="card-label">1 · Do</span><h3>Start with an action</h3><p>If a page needs three paragraphs of terminology before you know what to do, the page has work to do. Brali starts with the smallest useful move.</p></article><article class="card"><span class="card-label">2 · Understand</span><h3>Learn the mechanism</h3><p>The goal is not to borrow scientific vocabulary. It is to understand what the protocol is changing: attention, timing, friction, feedback, retrieval, environment, or another observable part of the situation.</p></article><article class="card"><span class="card-label">3 · Observe</span><h3>Watch a real signal</h3><p>Track the outcome the protocol is actually meant to change. A streak is not automatically progress, and a timer is not a biological law just because it has a reassuring countdown.</p></article><article class="card"><span class="card-label">4 · Bound</span><h3>Keep the claim smaller than the hype</h3><p>If a study supports recall, Brali should not quietly upgrade that to intelligence. If it supports an acute response, it does not become a longevity protocol on the walk from the abstract to the headline.</p></article></div><section class="prose"><h2>Why the trust labels exist</h2><p>Search visibility and recommendation trust are deliberately separate. Brali keeps the historical library discoverable so old material can be audited and improved, but people and AI agents should not receive an unreviewed page as if it had already earned the same confidence as a checked protocol.</p><p>That is why a page can exist publicly and still be review-gated. "We have a URL" is not an evidence state.</p><h2>The current quality model</h2><p>The library currently contains ${evidence.entries.length} entries. All ${indexing.indexable_count} public entries are included in the sitemap; ${indexing.trusted_recommendation_count} currently meet the trusted recommendation bar and ${indexing.review_required_count} remain review-gated.</p></section><div class="grid two"><article class="card"><span class="card-label">Reviewed · ${counts.reviewed ?? 0}</span><h3>Claims checked</h3><p>Evidence-like claims have traceable sources where required, and the practical guidance has completed editorial review. Reviewed does not mean immortal truth; it means the current wording survived an actual source check.</p></article><article class="card"><span class="card-label">Practical · ${counts.practical ?? 0}</span><h3>Low-risk practice</h3><p>The entry is framed as practical guidance and does not contain evidence-like claims that require scientific support. Not every useful action needs a lab coat.</p></article><article class="card"><span class="card-label">Pending review · ${counts["pending-review"] ?? 0}</span><h3>Searchable, not recommended</h3><p>The public page is crawlable and evidence-labelled, but it stays outside normal trusted recommendations until review is complete. A citation-shaped object is not yet a reviewed source.</p></article><article class="card"><span class="card-label">Restricted · ${counts.restricted ?? 0}</span><h3>Higher review bar</h3><p>Sensitive material remains visible for transparency and compatibility, with a clear safety boundary and no promotion into trusted recommendations.</p></article></div><section class="prose"><h2>What source review changes</h2><p>Research rarely arrives as a neat commandment. A review may support one outcome but not another; a trial may work in one population, for one duration, with several bundled components. Brali records those boundaries because they change the protocol. The interesting question is not simply <em>did the paper have a positive result?</em> It is <strong>what action does this result actually justify?</strong></p><p>When evidence is mixed, Brali can keep a bounded experiment, narrow an existing page, mark a source as a challenge, or decline to create a new hack at all. Finding a paper is the beginning of editorial work, not the ceremonial end of it.</p><h2>Current claim debt</h2><p>The claim gate inspected ${claimCounts.records_checked ?? 0} records and found review markers in ${claimCounts.records_with_markers ?? 0}. ${claimCounts.debt_entries ?? 0} records currently carry unresolved claim debt. <strong>${claimCounts.indexable_debt_entries ?? 0} unresolved claim-debt records are eligible for trusted recommendation.</strong></p><p>Claim debt is a review queue, not a verdict that a statement is false. Enforced categories block normal trusted recommendation when review is incomplete; monitor-only categories remain visible so editors can inspect broad effect, mechanism, and research-language wording without pretending a regular expression can replace source review.</p><ul>${claimCategoryRows}</ul><h3>Topic-level debt</h3><p>Claim debt currently spans ${topicDebtGroupCount} mapped Topic${topicDebtGroupCount === 1 ? "" : "s"}. ${claimCounts.topic_pending_debt_entries ?? 0} debt record${claimCounts.topic_pending_debt_entries === 1 ? " remains" : "s remain"} without a canonical Topic assignment. Topic counts are editorial workload signals, not evidence-strength scores.</p>${topicDebtRows ? `<ul>${topicDebtRows}</ul>` : "<p>No mapped Topic debt is currently reported.</p>"}<p><a href="/life-os/datasets/claim-debt.json">Open the complete machine-readable claim-debt report →</a></p><h2>How an entry earns trusted recommendation</h2><ol><li>Start with a concrete action or protocol.</li><li>Detect evidence-like wording, quantitative claims, effect estimates, high-confidence causal assertions, first-party results, guarantees, clinical-outcome wording, and sensitive topics.</li><li>Record usable external sources when factual support is required.</li><li>Review exact wording and sources rather than treating a recorded link as proof.</li><li>Rewrite the action so the mechanism and evidence boundary are understandable to a non-specialist.</li><li>Keep every public entry discoverable, but include only <strong>reviewed</strong> and <strong>practical</strong> entries in the Trusted Protocol Feed and normal AI recommendations.</li></ol><h2>Inherited corpus corrections</h2><p>The migrated corpus is preserved for provenance. When the same inherited claim appears across many entries, Brali can apply a reviewed editorial normalization before public pages and evidence states are generated. ${normalizations.rules.length} reviewed normalization rule${normalizations.rules.length === 1 ? " is" : "s are"} currently registered, affecting ${normalizations.changed_entries} source entr${normalizations.changed_entries === 1 ? "y" : "ies"} in this build.</p><p>These corrections are published with their rationale, review date, reviewer, source, and application count rather than silently rewriting the historical corpus.</p><h2>Health and mental health</h2><p>Brali's Growth Library is general educational material and a starting point for personal reflection. It is not medical diagnosis or treatment guidance. Sensitive entries receive a higher review bar and stay outside normal trusted recommendations until reviewed.</p><h2>Machine-readable transparency</h2><ul><li><a href="/life-os/datasets/protocols.json">Trusted Protocol Feed</a> — reviewed and low-risk practical protocols.</li><li><a href="/life-os/datasets/evidence.json">Evidence status index</a> — evidence state for the complete library.</li><li><a href="/life-os/datasets/review-queue.json">Review queue</a> — entries still awaiting editorial work.</li><li><a href="/life-os/datasets/claim-debt.json">Claim debt report</a> — claim categories, enforcement state, Topic aggregation, debt reasons, source presence, and review metadata.</li><li><a href="/life-os/datasets/indexing.json">Indexing policy output</a> — complete search visibility and the separate trusted-recommendation set.</li><li><a href="/life-os/datasets/editorial-normalizations.json">Editorial normalization register</a> — reviewed inherited-claim corrections and where they were applied.</li></ul><p>The static build fails if a public hack is missing from the sitemap, still carries <code>noindex</code>, loses its evidence state, or leaks a review-gated entry into the Trusted Protocol Feed. Quality gates are deliberately annoying in the useful way: they stop a nicer sentence from quietly becoming a worse claim.</p></section><div class="callout"><h3>Use Brali like a small experiment, not a belief system.</h3><p>Choose a relevant protocol, check its evidence state, try the smallest useful version in your context, observe the intended signal, and keep, change, or stop the practice based on what happens and what the evidence actually supports.</p><a class="button" href="/life-os/">Explore the Growth Library</a></div>`;

const schema = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  name: "Brali Growth Library content methodology",
  description: "How Brali turns research and practical ideas into understandable protocols while keeping evidence boundaries and trust labels explicit.",
  url: canonical,
  isPartOf: { "@type": "CollectionPage", name: "Brali LifeOS Growth Library", url: `${base}/life-os/` },
};

const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>How Brali thinks — content methodology</title><meta name="description" content="How Brali turns research and practical ideas into understandable protocols while keeping evidence boundaries and trust labels explicit."><link rel="canonical" href="${canonical}"><meta property="og:type" content="website"><meta property="og:title" content="How Brali thinks — content methodology"><meta property="og:description" content="Try the move, understand the mechanism, watch the signal, and know where the evidence stops."><meta property="og:url" content="${canonical}"><link rel="icon" href="/assets/images/brali-logo.png"><link rel="stylesheet" href="/styles.css"><script type="application/ld+json">${JSON.stringify(schema).replace(/</g, "\\u003c")}</script></head><body><a class="skip" href="#content">Skip to content</a><header class="site-header"><nav class="wrap nav" aria-label="Main navigation"><a class="brand" href="/"><img src="/assets/images/brali-logo.png" alt="Brali LifeOS"><span>Brali LifeOS</span></a><div class="links"><a href="/life-os/areas/">Life Areas</a><a href="/life-os/">Growth Library</a><a href="/life-os/datasets/">Datasets</a><a class="button yellow" href="/download/">Get Brali</a></div></nav></header><main id="content" class="page wrap">${body}</main><footer class="footer"><div class="wrap footer-row"><small>Brali LifeOS · How Brali thinks</small><div class="footer-links"><a href="/life-os/">Growth Library</a><a href="/privacy/">Privacy</a><a href="/support/">Support</a></div></div></footer></body></html>`;

const outputDir = path.join(root, "life-os/methodology");
await mkdir(outputDir, { recursive: true });
await writeFile(path.join(outputDir, "index.html"), html);

const libraryPath = path.join(root, "life-os/index.html");
let library = await readFile(libraryPath, "utf8");
if (!library.includes('/life-os/methodology/')) {
  library = library.replace('</h1>', '</h1><p class="meta"><a href="/life-os/methodology/">How Brali turns ideas into trusted protocols</a></p>');
  await writeFile(libraryPath, library);
}

const sitemapPath = path.join(root, "sitemap.xml");
let sitemap = await readFile(sitemapPath, "utf8");
if (!sitemap.includes(`<loc>${canonical}</loc>`)) {
  sitemap = sitemap.replace('</urlset>', `  <url><loc>${canonical}</loc></url>\n</urlset>`);
  await writeFile(sitemapPath, sitemap);
}

console.log(`Content methodology page generated with ${indexing.indexable_count} search-visible, ${indexing.trusted_recommendation_count} trusted-recommendation eligible, ${indexing.review_required_count} review-gated, ${claimCounts.debt_entries ?? 0} claim-debt record(s), and ${topicDebtGroupCount} Topic debt group(s).`);
